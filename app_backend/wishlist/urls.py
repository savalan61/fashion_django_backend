from django.urls import path
from .  import views

urlpatterns = [
    # path('toggle/', views.ToggleWishes.as_view(), name= 'add-remove-from-wishList'),
    # path('me/', views.GetWishList.as_view(), name= 'get-wishList')
]
